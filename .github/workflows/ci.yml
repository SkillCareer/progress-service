name: Service CI

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]

jobs:
  ci:
    uses: SkillCareer/progress-service/.github/workflows/reusable-ci.yml@main
    with:
      node_version: "18"
      image_name: "progress-service"
    secrets: inherit
